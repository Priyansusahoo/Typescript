/**
 * SYNTAX
 * let <variableName>: <type> = <initial value>;
 */
let tst: boolean = true;
console.log(tst);
tst = false;
console.log(tst);
/**
 * Unlike Javascript, typescript has type safety.
 * so we can't put different data types of data
 * in a variable except if it is declare as "any"
 */
